# systemd-boot menu: boot the normal Arch UKI by default, show the menu
# briefly so the rescue UKI stays one keypress away, and disallow editing.
default arch-linux.efi
timeout 10
console-mode max
editor no
